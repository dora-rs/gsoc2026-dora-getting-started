Reproduce this supplied project exactly in assets mode. Read only
`TUTORIAL_CONTRACT.md`, `VERSIONS.md`, and `ASSET_GUIDE.md`; summarize versions,
the single entry, and acceptance in at most 10 lines. Run exactly
`bash tutorial.sh run`. Do not edit, install, rebuild, change versions, or run
components directly. On failure stop and report its stage/log; do not
workaround. On success run `git status --short`, inspect documented outputs,
and report PASS/FAIL in at most 15 lines. Never expose secrets or machine data.
