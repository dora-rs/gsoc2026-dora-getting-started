# Week 2 Rerun Static Scene Verification

This directory verifies the static Rerun scene used in the tutorial.

```bash
./run.sh
```

The script creates a local virtual environment, installs pinned dependencies,
generates reusable glTF assets, writes `artifacts/dora_rerun_scene.rrd`, and
prints the verified software versions.

