Read only TUTORIAL_CONTRACT.md, VERSIONS.md, ASSET_GUIDE.md, and README.md.
Do not inspect implementation files unless the documented command fails.

Reproduce the supplied Habitat RGB and depth camera example without editing
source or substituting assets, dependencies, sensors, or launch commands.

In at most 10 lines, report the fixed environment, single entry command,
generated paths, and acceptance markers. Then immediately run the command in
ASSET_GUIDE.md.

After it exits, check only git status, required file sizes, image dimensions,
video codec/dimensions/durations, and the three markers. Return at most 15
lines and state PASS only when every check passes. Do not edit files.
