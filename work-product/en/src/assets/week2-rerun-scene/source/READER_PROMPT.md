Read TUTORIAL_CONTRACT.md, VERSIONS.md, ASSET_GUIDE.md, and README.md.

Reproduce the supplied static Rerun scene. Preserve both glTF models, all
object coordinates, the dataflow, pinned dependencies, and run.sh exactly.

First report the pinned versions, single entry command, expected generated
paths, and acceptance markers. Then run exactly the command in ASSET_GUIDE.md.

Do not edit source files, regenerate models, substitute assets, or use an
alternative command. If the command fails, stop and report the sanitized
error without changing versions or disabling checks.

On success, report sanitized versions, artifact dimensions and sizes,
acceptance markers, and source git status.
