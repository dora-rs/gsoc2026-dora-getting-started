Reproduce this supplied project exactly in assets mode.

1. Read `TUTORIAL_CONTRACT.md`, `VERSIONS.md`, and `ASSET_GUIDE.md` only.
2. Report fixed versions, the only entry, and acceptance markers in at most 10 lines.
3. Run exactly `bash tutorial.sh run`.
4. Do not edit, install, rebuild, change versions, or run components directly.
5. On failure, stop and report its stage/log. Do not workaround it.
6. On success, run `git status --short`, inspect documented outputs, and report in at most 15 lines.

Never display secrets or identifying machine/account information.
